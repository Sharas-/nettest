apiVersion: v1
kind: Service
metadata:
  name: nettest
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nettest
  sessionAffinity: None
  type: ClusterIP
